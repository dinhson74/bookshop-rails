<div class='center jumbotron'>
  <h1><%= I18n.t('home.welcome_to_the_bookshop') %></h1>
  <% if current_user.nil? %>
    <%= link_to I18n.t('home.sign_up_now!'), new_user_registration_path, class: "btn btn-lg btn-primary" %>
  <% end %>
</div>
